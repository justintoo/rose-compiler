#!/usr/bin/env bash

#--------------------------------------------------------------------
# Run in strict-mode
#--------------------------------------------------------------------
shopt -s extglob  # enable extended globs
set -o errtrace   # fail if any statement returns non-zero
set -o errexit    # fail if command fails
set -o nounset    # fail if variable is not set, i.e. "unbound"
set -o pipefail   # fail if any command in a pipeline fails

ROSE_SH_HOME="$(cd "$(dirname "$0")/.." && pwd)"
ROSE_SH_BIN_HOME="${ROSE_SH_HOME}/bin"
ROSE_SH_LIB_HOME="${ROSE_SH_HOME}/lib"

source "${ROSE_SH_LIB_HOME}/rose/rose.shinc"

rose::main ${@:---help}
